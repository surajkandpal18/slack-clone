import { createMuiTheme } from "@material-ui/core";

export const myTheme = createMuiTheme({
  palette: {
    primary: {
      main: "#3F0F3F",
    },
    common: {
      googleBlue: "#4285f4",
    },
  },
});
